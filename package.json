{
  "name": "showcase-brownstore-ecommerce",
  "private": true,
  "version": "1.0.0",
  "description": "BrownStore showcase e-commerce catalog",
  "scripts": {
    "dev": "concurrently -n server,client -c brown,cyan \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}

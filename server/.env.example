PORT=5000
NODE_ENV=development
MONGO_URL=mongodb://127.0.0.1:27017/brownstore
JWT_SECRET=change-this-to-a-long-random-secret
ADMIN_EMAIL=karim@brown.com
ADMIN_PASSWORD=karim0fadmin
API_KEY_PIN=246810
IBSHER_API=https://api.ibsher.com/api/v1/client
IBSHER_CDN=https://cdn.ibsher.com
# Product photos are served at /media/... on this site (proxied from IBSHER_CDN)
CLIENT_ORIGIN=http://localhost:5173
# Production: set MONGO_URL to the Mongo host, a long JWT_SECRET,
# ADMIN_EMAIL / ADMIN_PASSWORD, and CLIENT_ORIGIN to the live site URL.
# If nginx sits in front of the API, raise the body limit (default is 1m):
# client_max_body_size 10m;
